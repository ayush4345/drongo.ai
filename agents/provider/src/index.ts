// @drongo/agent-provider — the metered weather provider agent. Sells current
// weather per call via the keyless Open-Meteo API; priced and served through the
// @drongo/agent-core channel. The consumer agent buys this service.

export * from "./weather.js";
